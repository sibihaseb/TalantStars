# Environment Variables Template for GitHub + Supabase Migration

# Database Configuration (Supabase)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
PGHOST=db.[YOUR-PROJECT-REF].supabase.co
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=[YOUR-DATABASE-PASSWORD]
PGPORT=5432

# Stripe Payment Integration
STRIPE_SECRET_KEY=sk_test_[YOUR-STRIPE-SECRET-KEY]
VITE_STRIPE_PUBLIC_KEY=pk_test_[YOUR-STRIPE-PUBLIC-KEY]

# Email Service (Resend)
RESEND_API_KEY=re_[YOUR-RESEND-API-KEY]

# OpenAI Integration
OPENAI_API_KEY=sk-[YOUR-OPENAI-API-KEY]

# Session Management
SESSION_SECRET=[GENERATE-RANDOM-STRING-32-CHARS]

# Application Environment
NODE_ENV=development
PORT=5000

# File Storage (Keep Wasabi or migrate to Supabase Storage)
# Wasabi S3 (current)
WASABI_ACCESS_KEY_ID=[YOUR-WASABI-ACCESS-KEY]
WASABI_SECRET_ACCESS_KEY=[YOUR-WASABI-SECRET-KEY]
WASABI_BUCKET_NAME=talent
WASABI_REGION=us-east-1

# Alternative: Supabase Storage
# SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
# SUPABASE_ANON_KEY=[YOUR-SUPABASE-ANON-KEY]
# SUPABASE_SERVICE_ROLE_KEY=[YOUR-SUPABASE-SERVICE-ROLE-KEY]

# Optional: Analytics and Monitoring
# SENTRY_DSN=[YOUR-SENTRY-DSN]
# ANALYTICS_ID=[YOUR-ANALYTICS-ID]

# Instructions:
# 1. Copy this file to .env
# 2. Replace all [YOUR-*] placeholders with actual values
# 3. Never commit .env files to version control
# 4. Create separate .env files for different environments:
#    - .env.development
#    - .env.staging  
#    - .env.production